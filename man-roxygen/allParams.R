#' @param x Vector of statistics for which EBFs will be calculated.
#' @param index Vector of indices in \code{x}.  EBFs are calculated only for
#' those elements. If \code{NULL}, EBFs are calculated for all elements.
